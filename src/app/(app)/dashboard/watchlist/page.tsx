import { AppShell } from "@/components/shell/AppShell";

export default function WatchlistPage() {
  return (
    <AppShell title="Watchlist">
      <h1 className="text-2xl font-bold text-slate-900">Watchlist & Alerts</h1>
      <p className="mt-2 text-slate-700">Upgrade flow placeholder. Core calculation remains open without login.</p>
    </AppShell>
  );
}

